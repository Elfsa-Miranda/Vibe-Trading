# naive_limit_momentum_trap

Shows a naive close-return limit momentum path rejected by A-share
tradability and execution-return gates. Dry-run output is produced by the
shared deterministic fixture and production report/card builders.
